export type timeSlot = {
    id: string,
    date: string,
    time: string,
    type: string,
    slots_count: number,
    client_date: string,
    client_time: string,
    available_slots: null | number,
    total_slots: null | number,
}